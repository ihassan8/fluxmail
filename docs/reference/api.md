# API Reference

Auto-generated from source docstrings via mkdocstrings.

::: autoemail.autoemail.AutoEmail

::: autoemail.utils.EmailObject

::: autoemail.utils.EmailEnv

**`EmailInstance`** is a `namedtuple` with two fields:

| Field | Type | Description |
|-------|------|-------------|
| `relay` | `str` | Hostname of the SMTP relay server (e.g. `smtp.gmail.com`) |
| `domain` | `str` | Email domain for the relay (e.g. `gmail.com`) |

::: autoemail.utils.EmailInstance

::: autoemail.utils.AutoEmailException
